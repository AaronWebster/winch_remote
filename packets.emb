-- Packet definitions.

[$default byte_order: "BigEndian"]
[(cpp) namespace: "internal"]

enum Direction:
  UNKNOWN = 0
  FORWARD = 1
  REVERSE = 2
  STOP    = 3

struct Packet:
  0 [+4] UInt sequence_id
  4 [+1] Direction direction
